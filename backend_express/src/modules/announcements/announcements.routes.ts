// Public entry point for the announcements module.
// The legacy route remains behind this boundary while its handlers are
// extracted incrementally into controllers and services.
export { default } from '../../routes/comunicados.routes';
