// Public notification service for the notifications module.
// The implementation remains backward-compatible while consumers migrate
// away from the legacy services directory.
export { default } from '../../services/notification.service';
